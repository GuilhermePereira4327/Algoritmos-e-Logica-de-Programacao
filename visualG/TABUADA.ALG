Algoritmo "tabuada"

Var

   n, a, resultado : inteiro

Inicio

      escreva("Deseja a tabuada para qual valor? ")
      leia(n)
      
      a <- 0
      
      para a de 1 ate 10 faca
           resultado <- n * a
           escreval(n, " X ", a, " = ", resultado)
      fimpara

Fimalgoritmo